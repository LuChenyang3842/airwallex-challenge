const requestInvitePath = '/prod/fake-auth';
/// otherPath and other path2 is used to indicate how to handle new api in the future
const otherPath = 'xxx';
const otherPath2 = 'xxx2';
export default Object.freeze({
  requestInvitePath: requestInvitePath,
  otherPath: otherPath,
  otherPath2: otherPath2,
});
